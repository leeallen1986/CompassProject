/**
 * The standalone Next Best 5 pilot duplicated projects already presented through
 * the This Week action board and triggered a second, slower client request.
 *
 * Keep the component boundary in place while the recommendation lifecycle is
 * redesigned, but render nothing and make no request in the rep-facing release.
 */
export default function FullPotentialNextBest5() {
  return null;
}
