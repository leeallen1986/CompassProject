/**
 * Deployment provenance remains available on the dedicated admin diagnostics page.
 * It is intentionally not rendered as a fixed badge over normal sales pages.
 */
export default function DeploymentProvenanceBadge() {
  return null;
}
