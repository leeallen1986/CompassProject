# Copy to .env.test.local and replace with a dedicated non-production database.
# The database name must contain a standalone test, testing, or ci marker.
# Examples: compass_test, compass-testing, ci_compass.
TEST_DATABASE_URL=mysql://username:password@hostname:4000/compass_test
